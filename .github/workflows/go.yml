name: GoProgram

on:
  push:
    branches: [ "main" ]

env:
  anhao: "Atletico de Madrid"
  eisen: "Inter Milan"
  furong: "Barcelona"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Scope
        run: |
          echo "Anhao's favourite football club is '$anhao'"
          echo "Eisen's favourite football club is '$eisen'"
          echo "Furong's favourite football club is '$furong'"


    # runs-on: ubuntu-latest
    # steps:
    # - uses: actions/checkout@v4

    # - name: Set up Go
    #   uses: actions/setup-go@v4
    #   with:
    #     go-version: '1.20'

    # - name: Build
    #   run: go build -v ./...

    # - name: ls
    #   run: ls
